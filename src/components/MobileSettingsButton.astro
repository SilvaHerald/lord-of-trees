---
export interface Props {
  buttonClass?: string;
  svgClass?: string;
  id: string;
}

const { buttonClass = '', svgClass = '', id } = Astro.props;
---

<button
  id={id}
  class={`p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors ${buttonClass}`}
  aria-label="Settings"
>
  <svg
    class={`w-6 h-6 text-gray-700 dark:text-gray-300 ${svgClass}`}
    fill="currentColor"
    stroke="none"
    viewBox="0 -960 960 960"
    ><path
      d="M440-120v-240h80v80h320v80H520v80h-80Zm-320-80v-80h240v80H120Zm160-160v-80H120v-80h160v-80h80v240h-80Zm160-80v-80h400v80H440Zm160-160v-240h80v80h160v80H680v80h-80Zm-480-80v-80h400v80H120Z"
    ></path></svg
  >
</button>
